# --- Web framework & server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database & migrations ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async driver used by the app (repositories use AsyncSession)
psycopg2-binary==2.9.10  # sync driver used only by Alembic migrations (see alembic/env.py)

# --- Validation & settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Auth & security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 can't detect bcrypt>=4.1 (removed __about__), breaks password hashing
python-multipart==0.0.20

# --- AI layer (Gemini) ---
google-genai==0.5.0

# --- Environment ---
python-dotenv==1.0.1

# --- Scheduling (recurring transactions) ---
apscheduler==3.11.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
aiosqlite==0.20.0
