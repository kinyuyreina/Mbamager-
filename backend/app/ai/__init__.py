"""
Mbamager AI Package

Holds Gemini prompt templates used by app/services/ai_service.py. These are
kept as plain Python string constants (rather than the design docs in
app/prompts/*.md) so they can be imported and .format()-ted directly.
"""
